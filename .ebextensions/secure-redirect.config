files:

  "/etc/nginx/conf.d/https_redirect.conf":

    content: |

      server {

        listen 80;

        server_name _;

        return 301 https://$host$request_uri;

      }

files:

  "/etc/nginx/conf.d/https_redirect.conf":

    content: |

      server {

        listen 80;

        server_name _;

        return 301 https://$host$request_uri;

      }

